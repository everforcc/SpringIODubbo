dubbo:
  application:
    # 服务名
    name: dubbo-provider
  registry:
    protocol: zookeeper
    address: 127.0.0.1:2181
  protocol:
    # 通信规则,协议和接口
    name: dubbo
    port: 20880
  monitor:
    # 连接监控中心
    protocol: registry
